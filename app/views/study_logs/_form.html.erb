<%= form_for(@study_log) do |f| %>
  <% if @study_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study_log.errors.count, "error") %> prohibited this study_log from being saved:</h2>

      <ul>
      <% @study_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :learned_word %><br>
    <%= f.number_field :learned_word %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
